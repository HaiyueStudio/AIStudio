# Harness bridge instructions

- This is the only package allowed to import `@deepseek-ai/dsh-*` or `@deepseek-ai/cordis`.
- Keep upstream types private and normalize every payload to Studio contracts.
- Upstream upgrades require pin, license, lockfile, boundary, and compatibility-fixture review.
