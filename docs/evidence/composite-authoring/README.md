# Composite authoring and acceptance evidence regression

The production screenshot path copies state and pixels before asynchronous PNG encoding and persists a matching evidence bundle. The task lifecycle routes screenshot/state tick mismatch into the existing bounded repair path and retains terminal diagnostics. Plan validation rejects absent visual-analysis/FPS producers before approval.

The general prompt profile is 3.5.0. Appearance decomposition is always included; detailed composition/prefab guidance is retrieved through the production knowledge loader. The real authoring-tool regression builds a rounded body and six independently colored rounded parts, then verifies prefab capture, replication, local transforms, Undo/Redo and save/reopen.

See verification.json for exact scope and outstanding checks. Targeted tests, native paused/running capture and all 12 capability suites passed. Full check executed all 48 integration files: 226 passed, 2 failed. The failures are the fixed Windows benchmark-machine gate and a plan-review window check; neither is counted as passing. Diagnostic window instrumentation passed but did not resolve the original test failure.

Local command logs are retained beside this file. integration-results.json and failing collected TAP files preserve the original full-run outcomes. Generated tracked test-output files were restored to their pre-task contents after copying the current results here. This evidence does not certify fresh model generation, visual correctness of a generated game, or completed M14 product admission.
