# 工具失败反馈与有界修正 · 2026-09-14

截图中的 studio.plan.propose 因验收项字段无效而失败。原实现虽回传失败，但 summary/digest-only 丢失诊断；模型结束后，未解决工具错误会直接阻塞任务。

实现：保留有界、脱敏的结构化诊断和修正步骤；对 plan.payload-invalid、tool.arguments-invalid、task.preview-stop-required 在原回合反馈，必要时按任务 repairIterations 预算续处理。未解决错误跨续处理回合保留，成功后的对应 blocker 清除；原失败尝试不改写。计划校验明确错误字段、期望类型及未知字段。用户取消、待审批、未知提交结果和不可识别错误不自动续处理。所有新调用沿用既有审批/修订/预算链路。

验证：
- 编排层 46 个测试均通过：首次全量 42/46；4 个项目历史测试因 macOS /var 符号链接被既有安全检查拒绝，使用 TMPDIR=/private/tmp 单独重跑全部通过。未修改安全检查。
- 新增 6 个测试覆盖压缩结果反馈、截图字段错误、回合结束的自动修正入口、预算/取消/审批/不可修复错误边界，以及真实 commitToolBody 向后端回传失败后接收修正成功的路径。
- 编排包和应用构建成功；git diff --check 通过。
- 根检查通过 contracts、类型、边界、上游/候选包、协议 spike、快速评测；停在已有 M14 验证输入绑定不匹配。未更新证据绕过门禁。

未调用真实模型重新生成魔方；本次验证覆盖宿主执行链路与可控后端回传。应用未重启。
