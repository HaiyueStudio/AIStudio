# G10 go/no-go — 2026-08-25

结论：**NO-GO**。

唯一硬阻断是当前 Windows runner 上 Electron 43.2 的 GPU subprocess 以 `0xC0000135` 退出，导致 desktop renderer 与
browser-host-in-Electron 两条真实 WebGPU smoke 均失败。其余门禁为绿色：package 127/127、app focused 25/25、真实双 backend 2/2、
内置浏览器 WebGPU 黄金路径、故障矩阵、离线 bug bundle 完整性/篡改拒绝、根静态门禁和 0-vulnerability audit。

恢复 `GO` 的必要条件：在同一 implementation commit `aff013539c08972ce5c28e44117933c775072579` 上，以 sandbox、真实 GPU、
无软件回退方式完成两条 Electron smoke 2/2，并确认生成的 PNG candidate、renderer teardown 与 GPU process 正常退出。若必须修改应用或升级 Electron，
则产生新的 implementation commit，全部 G10 门禁与双真实 backend 必须重跑。

建议将 Electron/driver compatibility matrix、启动成功率、GPU process crash diagnostic 和专用 Windows CI runner 纳入下一里程碑；
不要将机器环境问题写成产品功能 PASS。
