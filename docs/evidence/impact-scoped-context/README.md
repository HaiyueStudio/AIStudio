# 按影响范围获取上下文：验证记录

本次修复移除生产提示词装配器默认的全投影/1000 项场景读取，接入编辑器选区提示和按需查询流程，并将场景投影限制为 16 KiB。总 prompt 维持 96 KiB 上限。

实测真实 ProjectWorkspace：全量场景 112,154 bytes；选中一个实体后场景切片 964 bytes，工具合同变化后的新会话完整 prompt 12,068 bytes。其他实体组件没有被带入 prompt。测试没有调用真实模型 API，不据此宣称模型已完成游戏创作验收。

专项验证：

- `focused.txt`：15 项，包含 1000 对象局部读取、目标切换、未发送缓存、分页真实游标、单项超大数据、超大/不完整增量、失败准备、总预算降级，以及原有重启/脱敏/工具合同一致性。
- `runtime.txt`：运行时整包 87 项通过（新增总预算用例前的一轮；最终专项包含该用例）。
- `host.txt`：对话宿主与原有场景接线 12 项通过。
- `real-scene.txt`：扩展后的真实文档大场景及新会话接线回归通过。

源码已构建。运行中的 AIStudio 未重启。

完整检查：能力验证 124/124 通过；合同、类型、依赖边界及评测 59、行为 33、工作区 5、工具 111、逻辑 23 项均通过。桌面集成 48 个文件，233 项通过、1 项失败：`product-electron.test.mjs` 在 `product-main.mjs:109` 原生鼠标焦点超时，与前次日志隔离验证记录相同。完整检查返回 1，未标记为全绿；详情见 `check.txt`、`integration-results.json` 和 `collected-14.tap`。

测试生成的既有 test-output 文件已恢复到本次工作前的字节内容，新的验证记录单独保存在此目录。
