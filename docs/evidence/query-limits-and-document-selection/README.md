# 查询额度与文档结果绑定

当前任务改动：

- 设置面板保存设备级的文档、工具/组件、场景对象和场景变更查询额度。默认分别为 12、50、100、100；支持运行时调整。
- 超额查询先进入等待用户确认。扩大后使用请求数量，维持上限则继续执行较小查询；同一任务的同一工具在确认范围内复用选择。跨任务不复用。并行等额查询只弹出一个问题。
- 确认写入原有持久化会话检查点，关闭/重启后可恢复；非法选项不授予额度，取消不执行查询。
- 数量额度与传输分页分离；一页最多 1000 项。文档搜索与工具目录提供有界字节分页及 nextCursor；场景查询复用现有分页。
- 文档搜索返回 JSON、resultRef 和 matches[].index。engine.docs.read({fromSearch:{resultRef,index}}) 由 Store 从保存的结果填入 id 和 bundleDigest。模型工具声明不再公开这两个输入字段。
- 搜索页保留最近 128 页，实例命名空间保证重启后的引用不会误指另一页。篡改返回的 JSON 不影响 Store 的冻结映射。相关文档仍可使用版本绑定的短 ref；底层兼容旧 id/digest 调用并给出明确格式/未知/不匹配诊断。
- 场景占位 ID 提示省略 scope.sceneId 查询当前场景，或复制查询结果中的真实标识。

用户截图的原因见 reported-id-mismatch.json：搜索返回的 b 被模型改写为 a；同一轮成功结果的文档库摘要始终相同，没有发生运行时文档切换。

验证：

- host-and-docs.log：18 项任务/查询确认测试与当时 9 项文档测试通过；覆盖扩大、维持、取消、并行、跨任务隔离、重启恢复及原有验收任务回归。
- document-selection.log：最终 13 项文档测试通过（包含前述 9 项）；覆盖服务端回填、格式错误、版本绑定、篡改隔离、引用过期和分页。
- regression.log：93 项 IPC、游戏工具与会话 UI 回归通过。
- window.log/window.json/settings.png：独立临时数据目录下的真实 Electron 生产窗口，修改文档查询额度为 30、保存并重载，设置仍为 30；同时验证原有通知偏好。
- build.log：最终应用构建通过；inventory.log：56 个正式集成测试文件收集正常。

上述去重共 124 项定向/回归测试以及 1 项真实窗口测试。未重启用户正在使用的 AIStudio，未修改其项目内容。

仓库总检查：合同、类型、依赖边界、候选包和 M12 quick 的 59 项测试通过；随后停在既有 M14 验收记录的 source binding 过期检查。当前源码摘要为 sha256:3104d99adfaad3ef6eb95c2334d1bb66ba2b4569531d0716e6f71b4e6f265a02，已存验证记录仍绑定 sha256:bf00db15ccfa9dc2cdcff7479ac3c7d9e00b29a5f05bb9f6e13c6e34b3558007。本次没有手改正式验收记录，也不声明整仓验收通过。详见 root-check.log。
