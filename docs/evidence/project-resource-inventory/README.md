# Project resource inventory and thumbnails

Project UI queries now select document-owned resources before pagination. Creation templates and unavailable preset descriptors remain available to the separate catalog workflow, but do not appear in the project inventory. Geometry components are listed once without the containing entity appearing as a second geometry. Actual PBR overrides replace the underlying default material entry. Script bindings and transforms are not fabricated into script/geometry resources. Texture assets remain discoverable in the Texture category even when their aspect ratio resembles an environment map. Existing source references, approvals, History and resource action validation are retained.

Cards show only a thumbnail and the project name (the filename for file assets). Selection opens the existing details pane, including source, status, configuration and actions. Details can be closed and keyboard focus returns to the item. Mesh and material names omit the internal component-type suffix. The grid adapts to the containing panel, rather than the outer window.

The renderer thumbnail owner uses the viewport's public Engine geometry factory and authoring parameters to draw bounded mesh edges. Material spheres use a small CPU lighting illustration of color, roughness and metallic values; they are not full Engine PBR renders and do not reproduce texture maps, normal maps or scene lighting. PNG/JPEG/WebP thumbnails read actual controlled bytes through existing asset/read IPC with identity/digest checks, preserve aspect ratio and close decoded bitmaps. Other formats and unavailable images keep a placeholder. There are at most two rendering jobs and 48 cached 128px canvases. Tab changes, project replacement and disposal abort pending publication. No project scripts or scene mutations run to produce thumbnails.

Regression coverage: empty inventory, actual authoring tools, geometry/entity distinction, effective PBR material, undo/redo, save/reopen, missing file and asset guards, project replacement, bounded pagination, IPC ownership, actual mesh/image pixels, material color differences, aspect ratio, compact cards, detail close/open, tab cache, narrow panels and cancellation/disposal. Final logs and counts are recorded in verification.json after checks finish.

The production-window authoring and restart phases passed. Its separate large-scene phase stopped at the existing frozen-machine assertion: the configured baseline is Windows/i7-7700/8 CPUs, while this host is macOS/i7-9750H/12 CPUs. No machine baseline or performance acceptance was changed. The current user app was not restarted and no live project was edited.

Final checks: build passed, all 21 resource/service/IPC checks and both resource window tests passed. Capability capture completed 17 groups, then the pointer-gestures group had 75 passed and 1 failed (278 passed overall). The native pointer fixture used its old 640x480 input size after requesting 700x450 and waiting 100 ms, consistent with resize synchronization timing. An independent unchanged-source rerun passed all 40 clicks plus its drag/capture/blur cases. The unsuccessful capture deliberately preserved the previous generated report. Root check then stopped at stale verification input binding; subsequent root stages were not reached. This is not a fully green repository verification. No source or acceptance evidence was rewritten to hide the failure.
