# M13 G01 old-architecture baseline

Binding: `m13-g01-2026-09-01`

The repeatable evidence is `m13-g01-baseline.json`, generated by `node scripts/measure-m13-g01-baseline.mjs` and checked with `--check`. It reads reviewed local artifacts and issues no provider request.

The current conversation host is 1,344 lines/114,201 bytes and awaits each tool request inside the event stream, so observed dispatch concurrency is one. Context uses a fixed 98,304-byte ceiling and retains at most 12 summary items; it has no model-capacity pressure, first-class Surface replace, manual compaction or provider-session recovery.

The deterministic 1000 Entity/200 Script corpus serializes to 142,973 bytes. Changing one entity produces another 142,981-byte full snapshot, while the equivalent bounded diff is 158 bytes (ratio 0.001105). This is a size baseline, not a claimed runtime speedup.

All seven game genres are present. Only the preserved snake/Codex and racing/Harness preflight failures contain attributable turn/token/cache/tool/wall-time records. The five unobserved genres remain `null/unknown`. Both observed failures preserved their project but captured no screenshot and ran no evaluator; therefore this evidence cannot be presented as acceptance or a completed 14-case matrix.

The runner also records the old recovery state: compaction, parallel batches and backend reconnect are unsupported; renderer restart restores a context digest but not the provider session; approval and unknown-outcome handling are partial.

