# Plan review 状态投影修复

方案请求的 `question.requested` 带 `barrierKind: plan-review`，确认的 `question.resolved` 只带相同 questionId 和 resolution。旧投影按每条事件是否携带 barrierKind 选择 `plan:` 或 `barrier:` 前缀，导致请求与确认落在两个节点，方案永久显示 waiting，并干扰当前执行位置和上下文压缩条件。

新投影在顺序重放时记住请求的 questionId → 节点坐标，将回答/取消合并回原节点，保留方案标题与内容，补上终态和结束时间。无需修改历史日志或审批协议。单纯出现模型文本不会清除真实未解决的问题。

- `focused.txt`：32 项通过；覆盖方案回答/取消、重启确认形态、重放一致性、任务分组、来源记录合并、当前执行位置，以及不同问题之间的隔离。
- `electron.txt`：真实 Electron 测试通过；详情浮层保持打开时，方案确认后从“等待用户”变为“已完成”，不产生重复方案节点；方案高亮结束，运行中回合高亮继续。

当前用户的 AIStudio 未重启，运行任务未中断。

能力验证 124 项通过。完整 check 在脚本恢复用例停止（工具套件 110/111）；用例自身记录约 17 分钟异常耗时，预期的 script.propose 结果未返回。该用例未改动，单独复验通过，保留原始失败与 retry 日志，不能将完整 check 标记为一次通过。逻辑与集成阶段另行补跑。

只读重放本机已有会话也验证了方案 completed、来源事件 2 条、无重复问题节点、无序列诊断；仅保存计数和状态，不复制用户会话内容。

补跑结果：逻辑 23/23 通过；集成 48 个文件、235 项通过、1 项失败。失败位于 `product-main.mjs:145` 的固定 Windows 机器配置断言，当前 macOS 环境不匹配；没有修改基准绕过检查。实际拓扑/详情浮层、审批恢复、方案审核和历史回放用例通过。完整检查及补跑未标记为全绿。

原有 test-output 文件已恢复到此次修改前的内容，本次证据单独保存在此目录。
