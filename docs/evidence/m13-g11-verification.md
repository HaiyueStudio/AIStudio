# M13 G11 local verification

Binding: `m13-g11-2026-09-02`

Status: local gates pass; the 14-case real-network matrix remains the formal release gate.

## Blocker correction

- Legacy `conversation/node-projected` and `conversation/task-projected` records are migrated before graph restoration.
- Migration operation ids are deterministic and resumable. A second startup does not append duplicate messages or replay historical mutations.
- The original task summaries, timeline and projected nodes remain in a redacted CAS artifact; the Session Surface contains the user/assistant Transcript needed for compaction.
- The conversation host claims its launch slot before asynchronous context preparation, so the UI cannot briefly report idle and race compaction or a second send against a not-yet-created Session.

## Local evidence

- `apps/ai-studio/test/legacy-session-migration.test.mjs`: identical Surface/Graph digests after replay, idempotent second migration, zero historical tool/document mutation operations.
- Existing restart, long approval, budget continuation, unknown outcome, transaction recovery, process claim, compaction and execution graph suites remain green.
- `docs/evidence/m13-g11-local-gates.json`: 126-entry long Transcript, automatic and manual compaction, 1000-node graph projection/layout, 1000 Entity/200 Script diff workload, 32-node parallel batch, memory evidence.
- Full Scene retransmission reduction: at least 80% gate; measured value is recorded in the JSON evidence.
- PTC model-turn reduction: at least 30% gate; local batch workload reduces three serial model scheduling turns to one.
- Input-token reduction uses the reviewed seven-game G10 A/B corpus and remains above 25%.

## Formal matrix contract

Every Harness/Codex × seven-genre report now persists:

- usage, cost and cache provenance;
- state, screenshot and evaluator evidence;
- Durable Session snapshot and replayed Surface digest;
- ContextFrame and exact Surface input artifact;
- Tool Batch evidence, including `tool.outcome-unknown` on interrupted results;
- execution Graph artifact and independently replayed Graph digest.

The formal verifier rejects any case missing one of these fields, any digest drift, any non-pass evaluator result, duplicate backend/genre coverage, dirty revision binding or fewer than 14 independent cases.

The first reviewed matrix attempt proved the failure-preservation path but also exposed an obsolete immediate hard stop at the initial Harness input tranche (1,020,271 versus 1,000,000 tokens). The corrective runner now treats the initial value as a visible checkpoint and continues the same preserved task only within an explicit formal-matrix cap (Harness 1.5M / Codex 3M input tokens). The continuation is recorded in `budgetContinuations`; exceeding that second cap still fails closed. This automation is limited to the already user-authorized formal matrix. Interactive Studio tasks continue to require the user's budget-continuation decision.

The next reviewed Harness/Snake canary crossed that initial tranche without stopping and exposed a narrower transaction gap: DeepSeek disconnected after three successful, validation-clean `script.propose` results but before it emitted `script.apply`. The coordinator now recognizes the latest full Studio-side `canApply` proposal, prepares an exact `script.apply`, requests the normal one-shot trusted-code approval, commits through the regular Script/History/Operation Log path, and records the recovered call in Session Tool Batch evidence. It never applies an invalid proposal, bypasses approval, retries a consumed proposal, or replays an ambiguous mutation. If all bounded provider takeovers still fail, the terminal record now retains `TRANSPORT` as the root cause instead of replacing it with a downstream gameplay-contract diagnostic.

The recovery canary then retained one enabled script and real Play/screenshot evidence, proving the proposal transaction fix, but all three bounded turns still ended on DeepSeek request-level `TRANSPORT`. The pinned Harness exposes a retry policy (including `TRANSPORT`, `TIMEOUT` and server failures) yet no listener previously owned `agent/request-error`, so that policy was inert and Studio paid for a whole continuation turn. The bridge now executes the upstream policy in the same Agent step with abortable deterministic backoff and a Studio hard cap of two retries. Authentication and other non-retryable errors remain terminal. The `script.propose` contract also states the stable persistent/input/instance APIs directly so agents do not spend Play cycles probing capabilities already owned by the runtime.

The fourth Harness/Snake canary proved that same-step transport retry: one backend turn created and applied a script, entered real Play, stepped simulation, injected input, inspected gameplay state and captured a PNG without a transport terminal. It then exceeded the reviewed 1.5M input cap by 9,857 tokens after 31 usage records and 40 tool requests. The retained trace showed two avoidable script repair loops: strict TypeScript rejected implicit callback parameters, followed by an invented `component.events` API. The generic `script.propose` contract now states both constraints before generation.

Formal-cap enforcement is now two phase. A usage event latches the cap without aborting the already-paid provider response; the next tool request is rejected before preparation or effect, the backend is cancelled, and the coordinator returns all prior full Studio-side results with a `budget.formal-cap` diagnostic. If the response ends without another tool request, the runner records the complete Session/ContextFrame/Tool Batch summary and then fails the case. The cap is unchanged, no unauthorized work runs after it, and failure preservation no longer degrades to an empty tool-result list. The full local G11 gate passes with 75 game-authoring tests, including this exact boundary regression.

The fifth Harness/Snake canary exercised that boundary in the real runner. At 1,544,554 input tokens it retained 41 completed tool results, one enabled script, the saved project, usage/cost/cache attribution, and a replay-stable M13 Session/Graph instead of returning an empty summary. Its trace contained no strict-TypeScript validation loop; the remaining cost came from low-level granularity: six serial entity creates, repeated component operations and three rejected attempts to attach material fields to non-geometry entities.

The generic tool surface now exposes `entity.create-many`, which creates 1-32 already-known geometry, light or empty entities atomically with initial transforms, one revision and one Undo/Redo entry. Its schema separates geometry items from non-geometry items so lights/empty cannot carry material fields. The formal runner also exposes the existing semantic/delta tools (`scene.query`, `scene.diff`, `scene.get-many`, `tool.search`, `component.configure`, `camera.author`, `transform.batch`, `script.symbols`, `script.patch`, hierarchy and prefab operations) instead of freezing agents onto the legacy low-level subset. A mixed geometry/light/logic regression proves atomic commit and pre-effect rejection. The local G11 gate now passes with 76 game-authoring tests; the G12 runner safety suite passes separately.

The sixth Harness/Snake canary proved the semantic-tool correction: the model replaced six serial creates with one `entity.create-many`, used two incremental `script.patch` calls rather than retransmitting the script, and reached real Play in 36 tool calls instead of the fifth canary's 45. It still stopped at 1,523,235 raw input tokens. Provider evidence shows 1,476,224 of those tokens were cache reads, leaving 47,011 net-new input; the old cap therefore classified a 96.9% cache hit as new context growth. The project, enabled script, state at tick 124, PNG, evaluator, usage/cost/cache, Session and replay-stable Graph were all preserved.

Budget accounting now retains raw input and cached input unchanged in the auditable usage/cost records, while `TaskBudgetV2.limits.inputTokens` controls net-new provider input (`total - cache read - cache write`). Missing cache-read evidence remains fail-closed and charges the full provider input; an unreported cache-write count is conservatively zero because no unverified tokens are deducted. Output, cost, wall-time, turn, tool, repair and observation limits are unchanged. The formal live boundary uses the same rule and records its basis plus raw/cache counters in any continuation evidence; the product label is explicit about “Net-new input tokens”. This is a semantic correction, not a cap increase.

The same trace exposed two avoidable protocol-only retries. `script.patch` now accepts `entityId`, `scriptId`, or both and rejects mismatched dual identity before effect. `preview.validate` now accepts an optional `baseRevision` and enforces the normal stale-revision guard. The full local G11 gate passes with 71 agent-runtime, 26 shell, 76 game-authoring and 11 Electron/restart tests; the standalone G12 runner safety suite passes 3/3.

The seventh Harness/Snake canary then showed that DeepSeek reports cache reads but leaves cache-write tokens unknown. The first implementation required both fields, so live and reconciled accounting fell back to 1,528,677 raw input even though 1,464,064 tokens were verified cache reads. This case again preserved 36 completed tools, one script, real Play state/PNG and evaluator evidence. The net-new rule now requires only cache-read evidence and treats an absent cache-write counter as zero; its regression proves the canary would consume 64,613 budgeted input while the raw 1,528,677 remains in the ledger.

The eighth Harness/Snake canary proved the corrected budget in a new clean revision: it reached 1,961,535 raw input with 1,923,200 verified cache reads and remained within budget at 38,335 net-new input. It stopped on `agent.tool-progress-stalled` after 36 completed tools, not on budget. The retained trace already had a committed script, a valid state/trigger observation shape, clean real Play, fixed steps, input injection, inspection and PNG capture; the model then repeated validate/start/stop cycles instead of yielding to the runner-owned hidden replay.

The formal runner now converts only that exact redundant-self-check terminal into a Studio verification takeover. The gate is genre-neutral and requires an enabled committed script, a valid static gameplay telemetry contract, zero Play errors, and successful `preview.validate`, `play.start`, `play.step`, `play.input`, `play.inspect` and `play.capture` results. Any missing prerequisite remains a failure. The original stall diagnostic stays in the report and a `g12.agent-verification-takeover` diagnostic records why hidden replay took ownership. Gameplay trigger normalization also recognizes the common authoritative `gameover` state as `game-over`/`terminal-state`; HUD text remains excluded. `play.start` now explicitly documents that a validated plan is one-shot and gameplay restart must use the game input.

The ninth Harness/Snake canary proved that takeover boundary: the task ended `completed`, retained 47 tool results, stayed within the unchanged input budget at 45,202 net-new tokens despite 2,395,538 raw / 2,350,336 cached input tokens, and entered the runner-owned hidden replay. Its evaluator still failed 0/5 because the runner flattened only the final gameplay observation. The retained final state itself proved partial functionality (`score=1`, `length=4`, collision state `over`), while direction changes, food transitions and restart were discarded. The same run exposed two independent acceptance defects: physical replay controls (`ArrowRight`, `KeyR`) were injected as action names instead of being resolved through the project's input action map (`right`, `restart`), and canvas-only capture omitted the DOM HUD. The PNG was also correctly found visually unusable: the vertical local-XY board plane occluded the top-down view.

The corrective implementation keeps every bounded authoritative observation, resolves all direct and semantic replay controls through the persisted action map, and adds a reviewed state-aware Snake driver that must actually reject an immediate reverse, collect two foods, collide and restart. The evidence analyzer derives deltas, transition counts, restart restoration and fixed-tick drift from that trace rather than trusting HUD text or the final frame. Screenshot capture now composites the WebGPU canvas and HUD into one same-tick PNG, then correlates real bitmap colors with named Scene role materials; a nearly uniform or role-free image triggers one bounded, genre-neutral camera/geometry repair. Plane authoring documentation now states the Engine convention: local XY, rotate X by -90 degrees for a horizontal XZ floor. Synthetic trace/pixel tests, action-map tests, full G11 local gates and a real Electron replay/HUD pixel test pass; a new clean reviewed canary is still required before promotion.

The tenth Harness/Snake canary proved that the screenshot path is no longer black: the retained PNG contains the live HUD, snake and food, and the case preserved 49 tools plus complete usage/cost/cache evidence (1,823,670 raw, 1,784,064 cached, 39,606 net-new input tokens; 34,496 output tokens; 20,199 cost micros). It failed before evaluation with `g12.semantic-snake-state-missing` because the generated Engine scene correctly published board state as `head/food/direction: {x,z}`, while the reviewed driver accepted only `c/r`, `column/row` or `x/y`. The semantic driver and evidence analyzer now normalize `x/z` without losing axis orientation: `z+` maps to down and `z-` to up, then physical controls still resolve through the project's action map. Dedicated XZ regressions, all 37 G12 contract tests, the full G11 local gate and the real Electron iframe/HUD capture pass. A new clean reviewed canary remains required before the 14-case matrix starts.

The eleventh Harness/Snake canary passed the XZ boundary and produced an enabled, error-free script plus the full Play tool chain, but the provider repeated two more validate/start/step/input/stop cycles until the 15-minute case timer fired. The failure path preserved the project, live screenshot, evaluator and attribution (2,481,024 raw, 2,430,848 cached, 50,176 net-new input tokens; 40,095 output tokens; 25,058 cost micros), but the concurrent timer finalizer raced the coordinator and reduced the reported `toolResults` to an empty list despite 45 completed requests in the Operation Log. The runner now stops the provider at the first event after a committed script, valid telemetry, clean Play and completed validate/start/step/input/inspect/capture chain, then transfers control to hidden replay. Caller timebox cancellation returns all completed Studio-side results, never reruns a mutation, and reserves the final two minutes of the unchanged 15-minute case budget for replay/evidence. Normal coordinator disposal still rejects. The new cancellation regression brings game-authoring tests to 77; all 37 G12 tests, the full G11 gate and real Electron replay capture pass. A clean reviewed canary is still required.

The twelfth canary showed why screenshot capture must be Studio-owned. The agent committed a script and completed validate/start/step/input/inspect, but never requested `play.capture`; it repeated Play cycles until the 13-minute authoring timebox. The Operation Log retained 38 requested tools and its last usage event (1,726,687 raw, 1,695,360 cached, 31,327 net-new input; 46,443 output), but the Harness stream did not settle after abort, so the parent killed the child at 16 minutes and could recover only disk checkpoints. The coordinator now emits every completed full tool result to a host retention hook. `runRecorded` races the provider stream against a two-second post-abort grace and synthesizes a Session-bound terminal summary from those retained results if the stream hangs; the aborted signal prevents any later effect. The proactive takeover no longer requires an agent-authored screenshot: committed script, valid telemetry, clean validate/start/step/input/inspect is sufficient, and the hidden replay then performs authoritative capture and visual evaluation itself. The next clean canary must prove both earlier termination and formal screenshot evidence.

The thirteenth canary proved bounded hung-stream recovery: it emitted its own `g12.agent-authoring-timebox-reached` terminal at 13 minutes instead of being killed by the 16-minute parent, retained 31 full tool results, one enabled script, a playing zero-error preview, screenshot/evaluator artifacts, replay-stable M13 evidence, and complete attribution (1,446,983 raw, 1,426,432 cached, 20,551 net-new input; 42,912 output; 18,886 cost micros). It did not enter hidden replay because the generated agent never called `play.input`, even though it had already completed validate/start/step/inspect/capture. Input injection, like screenshot capture, belongs to the independent hidden replay: requiring the authoring agent to duplicate it delays or blocks the very black-box validation intended to test input behavior. The handoff prerequisite is now only committed script, valid telemetry, clean validate/start/step/inspect; hidden replay owns all acceptance input and screenshot evidence. A new clean canary remains required.

The fourteenth canary proved the revised handoff by entering hidden replay after 26 tools instead of timing out (930,306 raw, 904,960 cached, 25,346 net-new input; 36,246 output; 16,231 cost micros). It then failed `g12.semantic-snake-state-missing` because this independently generated script published `head/food` as XZ vectors but `dir` as an opaque integer index. The integer contains no portable orientation contract, yet consecutive authoritative head positions do: the semantic driver now advances naturally until the head changes and derives the current vector from that transition. Every later action consumes the state returned by its own fixed-step window before requesting another step, avoiding overshoot at faster game cadences. The evidence analyzer applies the same transition-derived direction logic. Dedicated enum-direction and XZ tests pass; no model-visible prompt or enum mapping was added. A new clean canary remains required.

The fifteenth canary crossed the enum-direction boundary and again entered hidden replay without a wall-time stop, preserving 34 completed tools, an enabled script, Play observations, screenshot/evaluator fallback and attribution. It failed specifically at `g12.semantic-snake-movement-timeout`: the generated game published a valid vector direction but remained in authoritative `ready` state until a non-reverse direction was pressed, while the driver began with the reverse-rejection assertion. The retained agent Play trace also proved a handoff may arrive already `gameover` after author-side stepping. Hidden replay now normalizes only from published lifecycle state: it sends the project-mapped R control when terminal, sends a safe current/toward-food direction when waiting to start, observes real movement, and only then begins the independent reverse/food/collision assertions. Ready-start and terminal-restart regressions, all 40 G12 quick tests and the real Electron iframe control test pass. A new clean canary remains required before the 14-case matrix.

The sixteenth canary proved lifecycle normalization: it entered hidden replay after 26 tools and advanced the snake, but failed `g12.semantic-snake-early-terminal`. Its authoritative trace made the defect unambiguous: food was at board cell `(10,15)`, the head at `(12,10)`, yet the driver issued up and collided at row zero. The parser had conflated board `r/row`, Engine `z`, and Cartesian `y`: increasing row and increasing Z both mean down in the top-down gameplay controls, while increasing Cartesian Y means up. Coordinate parsing now preserves those sources and maps vertical controls accordingly. A dedicated increasing-row route regression passes along with all 41 G12 quick tests; no generated project or prompt was altered. A new clean canary remains required.

The seventeenth canary reached the strict verification-ready boundary after one recoverable transport turn and 27 retained tool results, but the runner recorded the internal `g12.agent-verification-ready` signal as infrastructure failure instead of entering replay. The earlier path worked only when the coordinator converted an observer exception into a terminal summary; when the same signal directly rejected a later recovery turn, `runRecorded` rethrew it before takeover normalization. The common turn boundary now synthesizes a Session-bound summary from the host-retained full results, rechecks the same committed-script/telemetry/validate/start/step/inspect/error-free predicates, and returns completed only if they still hold. Otherwise the original error remains fail-closed. The G12 runner tests and all 41 quick tests pass; a new clean canary remains required.

The eighteenth canary proved the takeover race fix and completed the full hidden replay in about four minutes. Functional evidence passed direction/reverse blocking, two food collections with growth/score/respawn, collision/restart restoration, and fixed-step stability; the evaluator reached 3/5. The two failures shared one screenshot defect: only a green body and score HUD were visible, while the food/boundary were outside the effective camera view and head/body were not visually distinct. The old repair gate admitted that image because it had 13 color clusters and exactly two visible materials, even though two state-correlated visual signals were false. First replay now requests one bounded generic visual repair whenever any independently analyzed visual signal is false, in addition to the coarse cluster/material thresholds. The second replay always proceeds to the formal evaluator so a still-bad image is recorded as acceptance failure instead of infrastructure failure. Runner and all 41 G12 quick tests pass; a new clean canary remains required.

The nineteenth canary entered the new visual repair path, but the parent watchdog ended it at 16 minutes and recovered only fallback evidence. The Operation Log proves the repair was active rather than lost: its second turn finally applied a script change at revision 13 only 11 seconds before the watchdog. Two structural causes were found. First, the 13-minute authoring timer had been cleared after initial generation, so post-replay repair had no bounded cancellation. It now covers every model turn and is cleared only by the outer case finalizer, retaining the final two minutes for replay/evidence. Second, visual/runtime repair no longer resumes the large authoring conversation; it starts a short Session against the same current project and task, with separate Session/usage/cost/cache evidence. A first failed visual replay is also carried forward as a complete attempt: if repair times out, fails, hits cap or removes telemetry, the runner emits the retained state/screenshot/evaluator result instead of degrading to parent fallback. All 41 G12 quick tests pass; a new clean canary remains required.

The twentieth canary completed initial authoring and entered hidden replay in about four minutes, then failed `g12.semantic-snake-movement-timeout`. The preserved script made the product defect precise: it declared `STEP_TICKS = 8` but compared its tick counter with an uninitialized `S.stepTicks`, so input changed the authoritative lifecycle from `ready` to `running` while `moveCount` remained zero forever. This was a generated-runtime semantic failure with no JavaScript exception, but the repair router only accepted thrown runtime errors, missing triggers and visual failures, so it incorrectly terminated before a repair Session. Generated-game `g12.semantic-*` failures now enter the same single bounded isolated repair path, while registry/control/observation/step invariant failures remain infrastructure errors. The repair receives a bounded summary of the last authoritative gameplay state and is told to verify initialized timing/configuration fields plus a real state transition after sufficient fixed steps. The initial prompt applies the same generic requirement without leaking hidden acceptance data. All 41 G12 quick tests pass; a new clean canary is required.

The twenty-first canary proved the isolated semantic-repair route: after the initial hidden replay rejected the observation shape, Studio opened a second Session with seven local context-artifact hits, and that Session completed 12 real tools in about 80 seconds. Its Play evidence showed input-driven movement, one food collection, score/length growth and restart. Two protocol gaps still discarded the result. The generated script published equivalent flat XZ telemetry (`headX`, `headZ`, `foodX`, `foodZ`) while the driver and analyzer only accepted nested points; both now normalize flat column/row, X/Y and X/Z variants while preserving vertical-axis semantics. The repair Session also returned the internal verification-ready terminal after satisfying the same strict script/telemetry/validate/start/step/inspect/error-free gate, but only the initial Session normalized that terminal. `runRecorded` now applies the takeover gate to every authoring and repair turn before persistence. Dedicated driver/analyzer regressions cover flat XZ, and all 43 G12 quick tests pass; a new clean canary remains required.

The twenty-second canary completed two replay-stable Sessions and the full hidden replay, producing formal state, screenshot and evaluator evidence instead of an infrastructure terminal. Four of five Snake acceptances passed: direction/reverse blocking, two food collections with growth/score/respawn, collision/restart and fixed-step stability. The remaining visual-role acceptance is a genuine generated-scene defect. Its 393x852 screenshot clips nearly the entire board and shows only edge fragments of the snake and food because orthographic size 4.3 was chosen from the short board axis without accounting for portrait aspect ratio; the Scene also has one shared `SnakeProto`, so head and body cannot be visually distinguished. The generic authoring and visual-repair contracts now require camera fit against both gameplay bounds at the target aspect ratio, plus semantically named, materially distinct entities/instance sources for independently readable active/controlled, trail/settled, target/collectible and boundary/background roles. These requirements are cross-genre and contain no hidden acceptance identifiers. A new clean canary remains required.

The twenty-third canary demonstrated that the cross-genre visual contract changed generation in the intended direction: the project used orthographic size 8.6 for the portrait target and created independently named/materialized head, body, food, board and four boundary entities. Hidden replay stopped earlier because the authoritative points used the standard `{col,row}` spelling, while the shared coordinate normalizer accepted `{c,row}` and `{column,row}` but omitted `col`. The semantic driver and evidence analyzer now use the same reviewed coordinate vocabulary for `c`, `col`, `column`, X/Y and X/Z, including corresponding flat field forms. This is a protocol alias fix and adds no model-visible prompt text. Dedicated driver/analyzer regressions pass, bringing the full G12 quick suite to 45 tests. A new clean canary remains required.

The twenty-fourth Harness/Snake canary on AIStudio `8695f17` retained 30 completed tools, two replay-stable Sessions, an enabled script, a live 393x852 PNG and complete usage/cost/cache attribution (746,494 raw, 717,184 cached, 29,310 net-new input; 34,031 output; USD 15,639 micros). Its generated script published the head as a grid point but exposed food only as a normalized pointer `interactionTarget`; a single screen fraction cannot authoritatively recover the board coordinate required for deterministic routing. The prior static gate accepted this incomplete shape, and the isolated repair Session therefore repeated Play without changing the script.

The correction replaces that permissive shape with generic gameplay telemetry schema v1: every observation declares `schemaVersion`, lifecycle state, authoritative transition events, `space`, `metrics`, stable-role `actors`, and stable-role `targets` with grid/cell or world coordinates. Normalized interaction targets remain optional pointer hints and cannot substitute for state. A shared trace index now normalizes nested scalar paths, events and role coordinates once for both hidden drivers and all seven evidence analyzers. Non-Snake analyzers are no longer empty, missing numeric evidence stays missing rather than defaulting to zero, common public transition vocabulary maps to reviewed replay triggers, the jigsaw replay now injects a real pointer cancel, and platform/racing replay explicitly exercises restart plus racing brake. Prompt profile v3 and the reusable `script.propose` contract expose the same genre-neutral schema. Root `npm run check` passes with 52 G12 tests; a clean reviewed canary is still required before formal promotion.

The twenty-fifth Harness/Snake canary on reviewed AIStudio `ac3b677` proved the authoritative telemetry correction: all hidden gameplay transitions passed, including direction/reverse rejection, two food collections with growth/score/respawn, collision/restart and fixed-step stability. It retained 53 completed tools, two replay-stable Sessions, state/PNG/evaluator artifacts and complete attribution (1,361,238 raw, 1,272,192 cached, 89,046 net-new input; 70,006 output; USD 35,630 micros). The evaluator reached 3/5; both failures came from one exact Scene defect. Every gameplay entity used PBR while the Scene had no light, so the real 393x852 bitmap rendered the distinct authored colors as black and could not correlate the food or role materials.

`preview.validate` now fails before Play with `tool.preview-light-required` whenever PBR or blinn-phong geometry has no directional, point, ambient or environment light. The error names the affected entities and offers two valid recoveries: add lighting or switch self-lit arcade roles to Basic. The entity tools and prompt profile v3.1 expose the same rule up front. Explicit screenshot and interaction wording now deterministically retains `play.capture` and `play.input` when the hybrid tool schema is compressed, preventing the richer descriptions from displacing required verification controls. The recovery regression passes before and after a light is added, all 79 game-authoring tests pass, and root `npm run check` passes with all 52 G12 tests. A new clean reviewed canary is required.

The twenty-sixth Harness/Snake canary on reviewed AIStudio `3b28c593` no longer produced an unlit-PBR frame: the generated arcade roles use Basic with distinct green/red colors. It retained 64 completed tools, two replay-stable Sessions, an enabled zero-error script, project/state/PNG/evaluator fallback and complete attribution (2,493,338 raw, 2,414,720 cached, 78,618 net-new input; 67,666 output; USD 36,713 micros). Hidden replay still failed before evaluation with `g12.semantic-snake-state-missing`. The preserved observation is valid schema v1 but uses another compositional form: `state:{status,phase}` and one actor `{id:'snake', role:'player', head:{cell}, dir}`, rather than a flat lifecycle plus a separately named `snake-head` actor. Static validation accepted it, while driver/analyzer role lookup required “head” in the actor identity and did not descend through `head.cell`.

The shared telemetry layer now resolves a stable-role entry once, walks bounded common point containers (`head`, `cell`, `grid`, `world`, `position`, `center`, `anchor`, `transform`), reads direction from the owning actor, and normalizes nested lifecycle state. Direction aliases also cover `dx/dy/dz` in addition to `dc/dr` and Cartesian vectors. Both hidden execution and evidence analysis consume these same helpers. Dedicated semantic-driver and analyzer regressions reproduce the exact nested player shape; all 54 G12 quick tests pass. A clean reviewed canary remains required.

The twenty-seventh Harness/Snake canary on reviewed AIStudio `bdf23a33` crossed the nested telemetry boundary and entered the growth route, then failed with `g12.replay-runtime-error`. It retained 50 full tool results (51 accounted tool calls), two replay-stable Sessions, enabled source, project/state/PNG/evaluator fallback and complete attribution (1,515,428 raw, 1,476,352 cached, 39,076 net-new input; 58,974 output; USD 26,116 micros). The preserved script made the remaining runtime contract defect exact: it called `api.scene.instances(BODY_SRC, bn)` where `bn = bodyCells.length - 1`. The preview runtime intentionally owns one pool per template and rejects a different capacity after growth, so a changing active count cannot also be the allocation capacity.

Script validation now rejects this pattern before commit with `script.scene-instance-capacity-dynamic`. A valid capacity must be a numeric literal or a recursively const-only numeric expression such as `COLS * ROWS`; gameplay changes only `setCount(activeCount)`. The tool contract, genre-neutral prompt profile v3.2 and formal runner prompt carry the same rule, while the validator remains the enforcing boundary. Regressions prove dynamic state-derived capacity fails and fixed computed capacity passes. Script-preview tests, all 80 game-authoring tests, all 54 G12 quick tests and root `npm run check` pass. A new clean reviewed canary is required.

The twenty-eighth Harness/Snake canary on reviewed AIStudio `4ba6bda6` proved the capacity correction in generated output: before the provider stopped, it created and committed one enabled Snake script with a fixed 225-instance body pool and active `setCount`, valid generic gameplay telemetry, Basic role materials, a horizontal board, action map and separate head/body/food templates. The provider then returned `QUOTA: Insufficient Balance` without usage counters or a terminal event. Studio recovered the validated script proposal, saved revision 7, retained 20 full tool results, three replay-stable turns, a forensic PNG and unknown-cost attribution, but incorrectly retried the same Session twice and finally surfaced the secondary `agent.stream-without-terminal` code instead of the causal quota diagnostic.

The runner now classifies `QUOTA` as non-retryable. If a validated enabled script and complete gameplay telemetry were already committed, Studio immediately records a verification takeover and lets the independent hidden Play path perform validation, fixed-step input, inspection and screenshots without another model request. If the artifact is incomplete, the case fails once with `QUOTA` as the root diagnostic while preserving all partial evidence. The exact runner regression and root `npm run check` pass. DeepSeek balance remains an external prerequisite for any cold case that has not produced a qualifying script before quota exhaustion.

The twenty-ninth Harness/Snake canary on reviewed AIStudio `e1f5ea9f` confirmed the replenished DeepSeek balance and crossed the full authoring/Play handoff: the case retained 41 complete tool results (42 accounted calls), two replay-stable Sessions, revision 5 with one enabled zero-error script, state, a real 393×852 PNG, evaluator fallback and complete attribution (1,369,444 raw / 1,299,968 cached / 69,476 net-new input tokens; 53,574 output tokens; USD 28,368 micros). Hidden replay failed with `g12.semantic-snake-early-terminal`. The authoritative trace and retained source showed that telemetry used Cartesian-style `cell:{x,y}` while the generated controls mapped ArrowUp to increasing `y`; the driver assumed increasing board row means ArrowDown and steered away from food into the wall. The isolated repair Session received only the terminal observation, so it could not reconstruct the causative input/state sequence and made no source repair.

Snake verification now performs one safe perpendicular black-box probe, learns the observed vector-to-control mapping from authoritative head deltas, derives the opposite control, and reuses the learned map for routing instead of assigning vertical meaning from field spelling. This keeps the hidden evaluator independent of generated coordinate conventions. Any semantic replay failure now retains bounded observations and the last injected inputs with ticks; the isolated repair prompt receives the first and last seven states plus the last sixteen inputs rather than only a terminal frame. Exact cell-XY inversion and failure-trace regressions pass. A clean reviewed canary is still required before starting the other genres.

The thirtieth Harness/Snake canary on reviewed AIStudio `18d4fea0` proved that correction end to end: all four authoritative gameplay acceptances passed, including calibrated direction/reverse rejection, two food collections with growth/score/respawn, collision/restart and long fixed-step stability. The run completed normally in two replay-stable Sessions with 36 tool calls and complete attribution (818,597 raw / 783,872 cached / 34,725 net-new input tokens; 42,243 output tokens; USD 18,884 micros). Its 4/5 evaluator isolated one genuine visual defect: the 393×852 bitmap showed food and a clipped diagonal boundary but no readable head/body. The isolated visual repair had set `orthographicSize=11.5` for a 20×14 board; at aspect 393/852 the required full vertical span is approximately 43.36 before padding.

Camera authoring now exposes a genre-neutral `frame-bounds` action. Given exact world minimum/maximum, plane, target width/height and padding, Studio atomically derives the aspect, target, safe viewing orientation and orthographic size as `max(verticalSpan, horizontalSpan/(width/height)) * padding`. Invalid or zero-area planar bounds fail before effect, and Undo/Redo remains one History entry. The generic prompt profile is version 3.4 and directs both initial authoring and bounded visual repair to this deterministic tool instead of model arithmetic. Tool description also clarifies that `orthographicSize` is a full vertical span, not a half-size. A clean reviewed canary must prove the generated agent actually uses the capability and reaches 5/5.

The thirty-first Harness/Snake canary on reviewed AIStudio `a335a566` did call the new `camera.author frame-bounds`, but its ratio-valued first contract still let the model invert 393/852 into 852/393; the resulting `orthographicSize=23.1` remained too small for the 20×20 board. The first hidden replay then found a separate generated semantic defect and opened an isolated repair Session. That Session patched and applied revision 6, ran Play/step/input, but reached the 13-minute authoring timebox before a final inspect, so the runner surfaced `g12.runtime-repair-not-completed`. It preserved 39 tool results, two replay-stable Sessions, revision 6, a live zero-error preview and attribution (1,199,347 raw / 1,139,456 cached / 59,891 net-new input tokens; 53,037 output tokens; USD 26,426 micros), but its fallback evaluator lacked the already captured replay trace and degraded to 0/5.

Two generic boundaries are corrected. `frame-bounds` now accepts `targetSize:{width,height}` rather than a precomputed ratio, eliminating width/height inversion. More importantly, any hidden replay that fails after authoritative observations now captures the current same-tick PNG, final observation, observed triggers, bounded trace and analysis into a retained `replayAttempt`. If a later repair fails, times out, hits budget or lacks final verification, the runner emits evaluator evidence from that pre-repair attempt instead of an infrastructure-only terminal. This directly advances the 14/14 failure-state/screenshot/evaluator requirement; a clean reviewed canary remains required.

The thirty-second Harness/Snake canary on reviewed AIStudio `1f6b6b46` proved that failure-retention boundary: after a completed semantic driver and a later lifecycle-trigger timeout, the bounded repair Session reached the case timebox, yet the run still emitted a same-tick 393×852 PNG and a formal 1/5 evaluator instead of an infrastructure-only failure. It retained two replay-stable Sessions, 52 tool calls and complete attribution (2,270,207 raw / 2,198,016 cached / 72,191 net-new input tokens; 65,809 output tokens; USD 34,688 micros). The evidence exposed two Studio defects rather than another prompt patch. First, `ProjectSceneAuthoringService.snapshot()` omitted the persisted project camera, so the isolated Preview silently used its default oblique camera even after a successful `camera.author frame-bounds`. Scene snapshots now carry the normalized project camera through ordinary edits, setting-only changes, save and reopen. Second, authoritative lifecycle value `state/status/phase = over` was not normalized to the replay's canonical `game-over` trigger even though all analyzers already treated it as terminal. The signal tracker now derives that genre-neutral alias from the authoritative lifecycle channel, allowing the queued restart to execute without requiring a model to guess hidden trigger spelling.

The thirty-third Harness/Snake canary ran on reviewed AIStudio `3ea6d5f` and proved the camera correction in a real 393×852 screenshot: the generated XZ board, head, body, food, border and HUD are now captured from a persisted top-down orthographic camera. The generated script was enabled, validation-clean and published the complete generic telemetry contract; the agent completed `preview.validate`, `play.start`, many fixed steps and two `play.capture` calls across 51 tool calls. It nevertheless reached the 13-minute authoring timebox and the formal report remained at base tick 1 with no semantic drivers, producing a preserved but non-replayed 0/5 evaluator. Root cause was a runner handoff mismatch: `play.step` had already persisted structured gameplay state and `play.capture` had persisted real screenshot proof, but verification takeover accepted only a separate `play.inspect`. The handoff now requires validate/start/fixed-step state plus either inspect or capture, while still requiring an enabled script, valid telemetry and zero runtime errors. This allows hidden replay to take ownership after equivalent independent proof instead of paying for repeated agent-side Play loops. Full root `npm run check` is green; a new clean canary is required before expanding the matrix.
