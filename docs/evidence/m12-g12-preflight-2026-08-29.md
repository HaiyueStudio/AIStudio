# M12 G12 preflight — 2026-08-29

Evidence class: `preflight`
Decision: `NO-GO`

This record is deliberately not a formal acceptance manifest. It documents runner readiness and the exact missing evidence before the expensive cross-genre matrix starts.

## Verified now

- AIStudio root `npm run check` includes `npm run m12:g12:quick`. The current credential-free G12 suite passes 27/27 tests, covers all seven genres, proves the blank control is 0/7, and rejects dirty/cross-revision/synthetic promotion.
- G08 contracts, candidate checks, package tests, app build and product runtime tests pass. The two Electron device tests initially failed because the restricted sandbox could not start Chromium's GPU child process; the same exact tests passed 2/2 in the authorized device environment, including the real WebGPU semantic pixel oracle and sandboxed blob-owner teardown.
- The real-backend smoke now supports `all`, `harness` and `codex` isolation so one backend failure cannot prevent the other from running.
- `npm run m12:g12:device` builds the desktop app and proves that a main-process `GamePreviewControl` can drive the real sandboxed iframe through start, rebased fixed-tick input, step, inspect, PNG capture and stop. The device run now also executes a reviewed semantic driver against real Electron/WebGPU, captures state and PNG at the same tick, and exits with cleanup residual `0`.
- Scripts can publish owner-scoped bounded JSON through `api.scene.observe`; `play.inspect` exposes it as authoritative gameplay state. Values are canonicalized, frozen, depth/size/count bounded and cleared on Play teardown. Runtime errors or missing gameplay observations fail replay execution.
- All seven hidden replays compile relative to the authoritative paused-start tick. The twelve semantic actions now have executable, bounded black-box drivers and a scheduler that prequeues fixed inputs, resolves only structured gameplay triggers, rejects stale ticks and never treats HUD text as outcome evidence.
- `AgentGameAuthoringCoordinator` can route real Backend streams and tool-result byte accounting through `AgentTurnRuntime`, closing the earlier gap where the minimal direct-backend smoke could not populate the G03 usage ledger.
- The serial cold-create matrix runner now enumerates Harness/Codex × seven genres, creates a fresh project and session per case, grants only allow-once tool approvals, performs the hidden replay in real Electron/WebGPU, and atomically checkpoints usage/cost/cache/state/PNG/evaluator artifacts after every case. Resume skips only previously passing pairs.
- Formal mode checks AIStudio, Engine and milestones cleanliness before any provider request. The current AIStudio and milestones worktrees are dirty, so the guard correctly rejects formal execution before spending the 14-task budget; preflight dry-run succeeds.
- Codex preflight passed independently with model `gpt-5.6-sol`, reasoning `low`, tool sequence `entity.create → transform.set`, terminal `completed`, document revision `3`, and no credential persistence.
- DeepSeek Harness first reached `project.snapshot → entity.create → transform.set` but its final provider request ended with a transport interruption. A single isolated retry passed with model `deepseek-v4-flash`, reasoning `low`, tool sequence `entity.create → transform.set`, terminal `completed`, document revision `3`, and no credential persistence.

## Still required for GO

- Seven independent blank-project cold-create tasks on Harness and seven independent blank-project cold-create tasks on Codex; project and conversation identities must never be shared.
- At least 6/7 first-pass complete tasks per backend; any remaining case must pass within the reviewed repair limit.
- For every task: effective model/config, prompt/tool/component versions, provider usage, cost state, cache provenance, hidden input replay, state and screenshot evidence, and per-acceptance evaluator result.
- Seven-genre Electron/WebGPU execution of the generated projects, not a reference fixture or pre-generated game template.
- Generated-project proof for all twelve semantic drivers across the seven genres. Driver code and the real iframe plumbing are complete, but the full cold-create matrix has not yet demonstrated every trigger and outcome.
- All 15 currently enumerated seeded defects repaired with before/after evidence within budget; false completion remains zero.
- v1 migration/rollback, save/reopen, Undo/Redo, Play restart, renderer reload, backend reconnect, app close, secret scan, artifact tamper rejection, budget continuation/stop, packaging smoke, and at least 30-minute soak with zero residual owners.
- One content-addressed `evals/evidence/g12/formal-acceptance.json` bound to the same clean reviewed AIStudio, Engine and milestones revisions. The formal verifier must recompute all referenced bytes and return `GO`.

## Current revision caveat

The three repositories are not a formal revision set while G12 implementation work and unrelated Engine shader work remain uncommitted. No result in this preflight may be promoted or combined with a later revision to claim completion.
