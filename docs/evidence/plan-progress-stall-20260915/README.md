# 计划节点滞后与恢复耗时诊断

分析窗口：2026-09-15 20:57–21:17（Asia/Shanghai），最新项目的“继续完整之前的任务”。只读分析日志与代码；未改变运行中的任务或工程。

## 核实结果

- 20:58:39 唯一成功的本计划 studio.plan.update 将步骤 1、2 设为 in_progress。此后没有成功的 completed 更新。
- 21:01:35 assembly.instantiate 已完成剩余 cubie 实例化；21:05:09、21:05:38 分两批创建 32+22 个贴片；21:06:11 创建灯光/控制器，21:06:36 配置游戏相机。说明静态内容已落地，不表示完整交互验收通过。
- 21:02:45 存在一次 studio.plan.update（updates:[]），在工具参数验证前被 budget.continuation-required 截断。空 updates 本身不是有效的完成进度报告，不能据此自动标记完成。
- 21:06:36 后实际工作已转向 pointer 配置、交互 API 查询和脚本提案。21:11:31–21:11:36 三个脚本在 r8 提案成功，但步骤 3 没有被标为 in_progress。
- 至 21:17 的窗口内去重后的 tool-result 共 110 条，记录执行耗时累计约 76.9 秒；静态相机落地后的约 10 分半内 60 条（包含该次相机调用），累计约 43.6 秒。其余墙钟时间包含模型生成、审批等待、预算暂停、恢复及调用之间的开销；本统计不能把它们全部归因于模型推理。

## 具体卡点

1. 进度依赖模型显式调用。applyPlanProgress 更新计划投影；拓扑直接使用 executionStatus，没有已完成产物与步骤的可靠对应关系。canonicalPlan 在每次恢复继续携带旧状态。这是状态协议缺口，并非节点 DOM 未刷新。
2. 重复审批：同一 transform.set 的参数摘要、预览摘要、目标和 r2 完全相同，20:59:06 与 20:59:39 都再次出现审批。交互主脚本的同一 r8 提案在 21:11:44、21:12:19、21:14:02 三次被批准 allow-once。窗口内共 13 次工具审批暂停及 2 次预算暂停；并非每次审批都重复。
3. 恢复分支 resolveRecoveredApproval 记录 approval.resolved、更新 UI 并要求模型重新调用；普通 resolveApproval 还调用 tools.decide。恢复分支没有同等地兑现工具侧授权，因此不能只用“用户批准过”的文本继续。
4. 分页失败：三次 scene.query 只回传 cursor、没有保留原 projection/scope。查询指纹不一致，返回 Scene cursor does not match the current query or revision range。额外引发场景重查。
5. 脚本修订串扰：三个独立脚本均在 r8 生成提案；21:16:34 一个脚本提交成功将文档升为 r9。21:17:03 另一个旧 r8 提案仅将调用 baseRevision 改成 9，仍被精确修订检查拒绝。随后继续搜索提案读取/重新基于当前版本提交的工具。

## 建议优先级

- 首先修复持久审批恢复：保留待执行操作，重新校验同一目标/参数/预览/修订，再兑现授权或说明失效原因；不能绕过校验，也不应让模型重新生成已批准操作。
- 计划步骤关联产物与当前操作；在阶段边界和恢复时核对步骤进度。不要依靠工具名称或看到物体就武断标完成。无确认更新时显示“状态待同步”，避免把后续脚本工作错误归到建模步骤。
- 查询返回可直接回填的 next-call 参数或宿主保存的查询句柄，避免模型只复制 cursor 丢失其余查询条件。
- 独立脚本保留提案源与目标依赖；支持统一审核/提交，或在目标脚本未变时重新验证、生成新提案。不能只篡改 baseRevision。

本轮仅完成原因诊断，未实施上述后续修复。
