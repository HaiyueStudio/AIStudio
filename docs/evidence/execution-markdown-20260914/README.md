# 执行节点 Markdown 详情 · 2026-09-14

根因：详情按纯文本渲染，上游还将多轮动作/结果用分号拼接。改为保留段落、按子节点分组，并在详情与完整记录中复用有界 DOM Markdown 渲染组件。HTML/媒体不执行或加载。

验证：50 项 shell 模型/图投影测试通过；独立 Electron 交互回归通过，确认段落、有序/嵌套列表、代码原文、引用、强调、无横向溢出和 HTML 不执行。已有图 UI 用例的阶段数量断言同步到此前新增的模型节点，并明确断言新增节点类型为 turn/model。组件和应用已构建。未重启用户应用。

截图含用于验证安全渲染的 HTML 和危险链接文本，均保持文本，未执行。

根检查通过合同、类型、边界和前置回归后，仍在 M14 验证输入绑定不匹配处失败；未重采证据或调整门禁。
